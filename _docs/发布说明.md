# HoCloth 发布说明

当前发布模型已经切换为“`Blender authoring + native MC2 bootstrap runtime`”。发布时会先整理出一个干净的 Blender 插件目录，再压缩为 zip。

相关文档：
- [本地打包流程.md](./本地打包流程.md)
- [发布流程与构建说明.md](./发布流程与构建说明.md)

## 当前推荐命令

```powershell
powershell -ExecutionPolicy Bypass -File .\package_addon.ps1 -Version manual-check -IncludeNativeBuild -CreateZip
```

## 预期产物

- staging 目录：`_build/release-<Version>/HoCloth`
- 发布 zip：`_dist/HoCloth-<Version>.zip`
- native 模块：`_bin/hocloth_native.cp311-win_amd64.pyd`

## 发布排除项

发布包不会包含这些开发期内容：

- `_native/`
- `_third_party/`
- `_docs/`
- `_build/`
- `_dist/`
- `package_addon.ps1`

## 说明

- 当前 native 已不再定位为“XPBD 占位实现”。
- 当前主线目标是先把 `Magica Cloth 2` 的三种模拟类型逐步复现进 Blender：
  - `BoneSpring`
  - `ClothBone`
  - `MeshCloth`
- 当前已经进入 `MC2 BoneSpring` 的第一轮 native 重构阶段，重点是把关键 C# 逻辑翻译到 C++，并保持 Blender 侧编译链路、实时播放链路和打包链路可用。
- 当前阶段不优先扩展 `MC2` 没有的功能，优先把 `MC2` 本身复现完整。
# 2026-04-24

- 继续沿 `MC2 BoneSpring` 路线推进，不扩展 `MC2` 之外的新能力。
- 补齐 `SpringConstraint` 参数链：`useSpring / springPower / limitDistance / normalLimitRatio / springNoise`。
- Blender authoring、compiled scene、native binding、native solver 已经串通。
- native 新增一段按 `MC2 SimulationManager.Spring(...)` 思路翻译的 spring 夹紧与回弹逻辑。
- 新增文档：`_docs/MC2_BoneSpring_第二轮复现记录.md`

# 2026-04-25

- 继续只做 `MC2 BoneSpring`，没有扩展到 `MC2` 外的功能。
- BoneSpring authoring 参数开始从平铺字段改成 `MC2` 风格的约束分组：
  - `spring_constraint`
  - `collider_collision_constraint`
- 预设、compiled scene、native binding 已切到新的分组参数路径。
- native 补入 `ColliderCollisionConstraint` 中 BoneSpring 的“碰撞推离后最大离原点距离限制”。
- 新增文档：`_docs/MC2_BoneSpring_第三轮复现记录.md`
