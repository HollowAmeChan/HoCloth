# 目录功能说明

本文档描述 HoCloth 当前目录职责。当前主线已经切换为“完整移植 Magica Cloth 2 Core 到 C++ native backend，再接入 Blender”。

## 根目录

```text
HoCloth/
  __init__.py
  authoring/
  components/
  compile/
  runtime/
  _native/
  _ReferenceProject/
  _third_party/
  _docs/
  _bin/
  _build/
  _dist/
```

## `authoring/`

Blender 侧交互层，负责选择提取、operator、handler、gizmo、工具和视口辅助。

它不负责物理求解，也不应保存 native runtime 内部状态。

## `components/`

Blender 组件与属性组织层，负责把用户可编辑数据组织为组件容器。

典型内容：

- BoneSpring / ClothBone / MeshCloth 组件
- collider / collider group
- wind
- preset
- UI 可保存参数

## `compile/`

Authoring 数据到 compiled scene 的转换层。

目标是输出稳定、扁平、可序列化、可 debug dump 的中间数据，供 native backend 构建 manager、buffer、constraint 和 virtual mesh。

## `runtime/`

Blender 与 native backend 的运行时桥接层。

职责：

- session 生命周期
- build / reset / step
- 每帧 transform、dt、风、override 输入
- 骨骼或网格结果回写
- cache / debug dump 调用

## `_native/`

C++ native backend。当前新方向要求它按 MC2 Core 的系统架构重建，而不是继续把 XPBD 逻辑堆在少数 cpp 文件里。

推荐结构见：

```text
_docs/MC2_Core完整移植重构方案.md
```

现有 `_native/src/hocloth_*.cpp` 只作为历史桥接参考，不再作为新物理核心的架构基础。

## `_ReferenceProject/`

参考源码目录。当前最重要的参考是：

```text
_ReferenceProject/MagicaCloth2/Scripts/Core
```

移植时必须优先对照这里的目录和源码文件。

## `_third_party/`

第三方依赖或本地依赖缓存目录。

## `_docs/`

设计说明、推进记录、移植计划、发布说明和历史记录。

当前优先阅读：

```text
工程大纲.md
ai须知.md
_docs/MC2_Core完整移植重构方案.md
```

## `_bin/`

native 构建产物或发布时需要携带的二进制文件。

## `_build/`

本地构建、打包 staging、临时产物目录。

## `_dist/`

发布 zip 等最终分发产物目录。
## Blender / Native 数据交换契约

当前 Python 与 native 的 build / frame / output / debug 数据统一由以下模块约束：

```text
runtime/exchange.py
_docs/Blender_Native数据交换契约.md
```

`compile/` 负责产出稳定 compiled scene；`runtime/inputs.py` 负责产出逐帧 frame inputs；`runtime/bridge.py` 负责把 exchange envelope 转换成当前 native 可读的 legacy payload。后续 native v2 parser 完成后，bridge 可以改为直接传 envelope payload。
